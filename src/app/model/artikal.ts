

class Artikal {
    sifra: string;
    naziv: string;
    kolicina: number;

    constructor(sifra: string, naziv: string, kolicina: number) {
        this.sifra = sifra;
        this.naziv = naziv;
        this.kolicina = kolicina;
    }
}

export default Artikal;